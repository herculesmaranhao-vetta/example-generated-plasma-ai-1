---
id: wi-001
phase: A
title: Atom wrappers — Badge, Button, Icon, Input, Progress, Skeleton, Tag
description: Create module-local wrappers for all atom-level $types needed by Pokedex UI
---

## Acceptance Criteria

- [x] Create `Badge` atom wrapper with ds-badge class and SMS DS token mapping
- [x] Create `Button` atom wrapper with ds-button + ds-button-\* variant classes, supporting PRIMARY, SECONDARY, GHOST variants
- [x] Create `Icon` atom wrapper wired to Fontawesome (ds-icon)
- [x] Create `Input` atom wrapper with ds-input class
- [x] Create `Progress` atom wrapper with ds-progress class for stat bars
- [x] Create `Skeleton` atom wrapper with ds-skeleton class for loading states
- [x] Create `Tag` atom wrapper with ds-tag class for type badges
- [x] Create `Avatar` atom wrapper with ds-avatar class for Pokémon images
- [x] Create `Avatar` atom wrapper with ds-avatar class for Pokémon images
- [x] All wrappers export properly typed React components with destructured props
- [x] All wrappers use SMS DS className conventions (ds-\* classes)
