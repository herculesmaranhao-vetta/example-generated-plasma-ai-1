---
id: wi-002
phase: A
title: Atom wrappers design fix
description: Ensure all atom wrappers use correct ds-* classes, tokens from tokens.ts/tokens.css, and FA Pro Regular only
---

## Acceptance Criteria

- [x] Badge: uses ds-badge class, no raw hex in styles
- [x] Button: uses ds-button + ds-button-{variant} classes, no raw hex in styles
- [x] Icon: uses FA Pro Regular only (data-prefix="far"), no @ant-design/icons
- [x] Input: uses ds-input class, no raw hex in styles
- [x] Progress: uses ds-progress class, no raw hex in styles
- [x] Skeleton: uses ds-skeleton class, no raw hex in styles
- [x] Tag: uses ds-tag class, no raw hex in styles
- [x] Avatar: uses ds-avatar class, no raw hex in styles
- [x] Tabs: uses ds-tabs class, no raw hex in styles
