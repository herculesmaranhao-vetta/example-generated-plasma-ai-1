import type { ThemeConfig } from 'antd';

/**
 * Resolved via: generate-component-tokens (FRAMEWORK=antd)
 * Source aliases: sms-ds/tokens/component/<name>.json
 * Semantic resolution: sms-ds/tokens/semantic/theme/light-theme.json + branding/plasma.json
 */
export const antdTheme: ThemeConfig = {
  token: {
    colorPrimary: '#0775be',
    colorInfo: '#0775be',
    colorLink: '#0775be',
    colorPrimaryHover: '#00539A',
    colorError: '#E3000F',
    borderRadius: 0,
    fontFamily: "Roboto, 'Helvetica Neue', Arial, sans-serif",
  },
  components: {},
};
