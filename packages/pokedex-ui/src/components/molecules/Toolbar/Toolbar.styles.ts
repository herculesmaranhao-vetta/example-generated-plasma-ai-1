import styled from 'styled-components';

export const StyledToolbar = styled.div`
  &.ds-toolbar {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 12px;
    padding: 16px 24px;
  }
`;
